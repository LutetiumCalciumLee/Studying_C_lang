#include <stdio.h>

void main()
{
    char ch;

    printf("Please enter a string: ");
    ch = getchar();
    putchar(ch);

    ch = getchar();
    putchar(ch);

    ch = getchar();
    putchar(ch);
}