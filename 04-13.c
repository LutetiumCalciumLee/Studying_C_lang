#include<stdio.h>

void main()
{
	int a = 10;
	printf(" Shifting %d left by 1 results in %d \n", a, a << 1);
	printf(" Shifting %d left by 2 results in %d \n", a, a << 2);
	printf(" Shifting %d left by 3 results in %d \n", a, a << 3);
}